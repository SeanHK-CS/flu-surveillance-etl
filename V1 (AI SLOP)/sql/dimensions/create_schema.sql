-- Create dimensions schema
-- Run this script before creating dimension tables

CREATE SCHEMA IF NOT EXISTS dimensions;

-- Grant permissions (adjust as needed for your database users)
-- GRANT USAGE ON SCHEMA dimensions TO your_user;
-- GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA dimensions TO your_user;
