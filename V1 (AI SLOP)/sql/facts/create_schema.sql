-- Create facts schema
-- Run this script before creating fact tables

CREATE SCHEMA IF NOT EXISTS facts;

-- Grant permissions (adjust as needed for your database users)
-- GRANT USAGE ON SCHEMA facts TO your_user;
-- GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA facts TO your_user;
-- GRANT EXECUTE ON ALL FUNCTIONS IN SCHEMA facts TO your_user;
